(function () {
  "use strict";

  var Hash, hash1;

  Hash = function () {
    var table;

    table = [];
    //TODO private method: loseloseHashCode(key) takes the key value and adds
    // the character value of each character in the key up and sets hash as modulo 37(%37)



    return {
      //TODO put(key, value) adds a new item to the hash table(or update it)
      //TODO remove(key) removes the value from the hash table using the key
      //TODO get(key) returns a specific value searched by the key
      //TODO handle collisions / multiple same hash values

    };
  }
}());